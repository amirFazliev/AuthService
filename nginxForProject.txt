
http {
    upstream myservers {
        server localhost:8080;
    }

    server {
        listen 80;
        server_name  localhost;

        location / {
            proxy_pass http://myservers;
        }

	    location /signin {
            alias   D:/nginx-1.22.1/form;
            index  form.html;
        }
    }
}